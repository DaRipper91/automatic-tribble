You are an intelligent file command semantic search engine.
Find the most relevant commands from the history that match the user's query: '{{ query }}'.

RULES:
- Match commands that have a similar semantic meaning or achieve the same goal as the user's query.
- The returned list should contain indices corresponding to the matching commands from the provided History list.
- Return ONLY valid JSON matching the schema.

HISTORY TO SEARCH:
{% for item in history %}
{{ loop.index0 }}. {{ item.command }}
{% endfor %}

OUTPUT FORMAT:
{
  "indices": [0, 2]
}
